apiVersion: score.dev/v1b1
metadata:
  name: bloog-editor
containers:
  main:
    image: .
    args:
      - editor
    variables:
      BLOOG_STORE_URL: ${resources.env.BLOOG_STORE_URL}
    resources:
      requests:
        cpu: 500m
        memory: 128M
    readinessProbe:
      httpGet:
        port: 8080
        path: /
service:
  ports:
    web:
      port: 8081
      targetPort: 8080
resources:
  env:
    type: environment
  dns:
    type: dns
  route:
    type: route
    params:
      host: ${resources.dns.host}
      path: /
      port: web
